server:
  port: 8888

spring:
  application:
    name: gateway-service
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml
        shared-configs:
#          - dataId: db-common.yml
#            group: "DEFAULT_GROUP"
#            refresh: true
          - dataId: nacos.yml
            group: "DEFAULT_GROUP"
            refresh: true
    sentinel:
      transport:
        dashboard: localhost:8080
logging:
  level:
    com:
     alibaba:
      cloud:
        nacos:
          client: warn