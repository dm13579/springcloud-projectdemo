server:
  port: 9999

spring:
  datasource:
    url: jdbc:mysql://122.51.157.42:3306/demo?serverTimezone=UTC&useSSL=false&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: 7028858@dm
    druid:
      initial-size: 5 #连接池初始化大小
      min-idle: 20 #最小空闲连接数
      max-active: 50 #最大连接数
      web-stat-filter:
        exclusions: "*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*" #不统计这些请求数据
      stat-view-servlet: #访问监控网页的登录用户名和密码
        login-username: druid
        login-password: druid
  redis:
      host: 122.51.157.42 # Redis服务器地址
      database: 1 # Redis数据库索引（默认为0）
      port: 6379 # Redis服务器连接端口
      password: 70111111@dm # Redis服务器连接密码（默认为空）
      jedis:
        pool:
          max-active: 8 # 连接池最大连接数（使用负值表示没有限制）
          max-wait: -1ms # 连接池最大阻塞等待时间（使用负值表示没有限制）
          max-idle: 8 # 连接池中的最大空闲连接
          min-idle: 0 # 连接池中的最小空闲连接
      timeout: 3000ms # 连接超时时间（毫秒）
  application:
    name: usercenter-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
mybatis:
  mapper-locations: classpath*:com/**/mapper/*.xml

jwt:
  keyPairName: jwt.jks
  keyPairAlias: jwt
  keyPairSecret: 123456
  keyPairStoreSecret: 123456